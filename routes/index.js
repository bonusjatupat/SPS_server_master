const home = require('./home');
const user = require('./user');
const parking = require('./parking');
const reservation = require('./reservation');
const reservationProcess = require('./reservationProcess');

module.exports = { user, home, parking, reservation, reservationProcess };